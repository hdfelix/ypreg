%table.table.table-bordered.table-hover.table-striped
  %thead
    %tr
      %th Name
      %th Location
      %th Type
      %th Event Dates
      %th Registration Window
      %th Cost
  %tbody
    - events.each do |event|
      - @event = event.object
      %tr
        %td= link_to event.name, @event
        %td= event.location_name
        %td= event.event_type
        %td= event.dates
        %td= event.registration_window
        %td= event.registration_cost
